# JournalSmart for QBO - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Flask Configuration
# =============================================================================

# Secret key for session encryption (generate a random string)
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here-change-in-production

# =============================================================================
# App Password (Optional)
# =============================================================================

# Set a password to protect access to the application
# Leave empty to disable password protection
APP_PASSWORD=

# =============================================================================
# QuickBooks Online Configuration
# =============================================================================

# Get these from https://developer.intuit.com/
# 1. Create an app in the Intuit Developer Portal
# 2. Copy the Client ID and Client Secret
# 3. Set the Redirect URI to match QBO_REDIRECT_URI below

QBO_CLIENT_ID=your-quickbooks-client-id
QBO_CLIENT_SECRET=your-quickbooks-client-secret

# OAuth callback URL (must match what's configured in Intuit Developer Portal)
# For local development: https://localhost:5000/callback
# For production: https://your-domain.com/callback
QBO_REDIRECT_URI=https://localhost:5000/callback

# Environment: 'sandbox' for testing, 'production' for live data
QBO_ENVIRONMENT=sandbox

# =============================================================================
# Account Mappings
# =============================================================================

# JSON array of mapping rules
# Each mapping has: pattern, from_account_id, to_account_id
# Pattern: text to match in journal description (case-insensitive)
# from_account_id: QuickBooks account ID to match
# to_account_id: QuickBooks account ID to change to
#
# Example:
# ACCOUNT_MAPPINGS=[{"pattern":"Office Supplies","from_account_id":"123","to_account_id":"456"}]
#
# Note: In Phase 2, mappings will be managed via the web UI and stored in database
ACCOUNT_MAPPINGS=[]

# =============================================================================
# Logging
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# =============================================================================
# Database (Phase 2 - Future)
# =============================================================================

# Path to SQLite database file
DATABASE_PATH=./data/journalsmart.db
